# ── Training pipeline ──────────────────────────────────────────────────────
# Data generation (local)
Pillow>=10.0.0
faker>=19.0.0
albumentations>=1.3.0

# Model training (install before running Colab notebooks locally)
# torch>=2.0.0
# torchvision>=0.15.0
# timm>=0.9.0

# Model deployment (inference)
onnxruntime>=1.16.0
onnx>=1.14.0

# Dataset upload / download
gdown>=4.7.0
# google-auth>=2.17.0
# google-auth-oauthlib>=1.0.0
# google-api-python-client>=2.86.0
